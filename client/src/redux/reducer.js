const inicialState = {
  allDogs: [],
  allTemperaments: [],
};

const reducer = (state = inicialState, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default reducer;
